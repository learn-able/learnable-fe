import styled from 'styled-components';

const Nav = styled.nav`
  background-color: red;
  height: 5rem;
  width: 100vw;
`;

const Header = () => {
  return (
      <Nav />
  )
}

export default Header;
